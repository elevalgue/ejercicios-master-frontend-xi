console.log('************** PRACTICE *********************');
console.log('05 - Fibonacci');

let firstValue = 0; 
let secondValue= 1; 
let count = 0;

const fib = (n) => {
while ( number <= n ) {
  [firstValue, secondValue] = [firstValue + secondValue, number];
}
  return number
}

console.log(fib(5));